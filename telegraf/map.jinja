{% import_yaml 'telegraf/defaults.yaml' as default_settings %}

{% set os_family_map = salt['grains.filter_by']({
        'Debian': {},
        'RedHat': {},
  }, merge=salt['pillar.get']('telegraf:lookup')) %}

{% do default_settings.telegraf.update(os_family_map) %}

{% set telegraf = salt['pillar.get']('telegraf', default_settings.telegraf, merge=True) %}
